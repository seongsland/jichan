{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Frontend (Vite)",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/frontend"
        },
        {
            "name": "Backend (Spring)",
            "type": "node-terminal",
            "request": "launch",
            "command": "./gradlew bootRun",
            "cwd": "${workspaceFolder}/backend"
        }
    ]
}